# Copy this file to .env and fill in your real key.
# .env itself is gitignored and never committed - .env.example IS committed
# so teammates know what variables they need to set.

OPENAQ_API_KEY=your_openaq_api_key_here
